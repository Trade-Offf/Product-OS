# PRD 黄金模版

> 这是给 AI 的指令集，不是普通填空题。花括号 `{...}` 中的内容是 AI 必须执行的动作。

---

# {项目名称} 产品需求文档

## 文档信息

| 项目     | 内容                |
| -------- | ------------------- |
| 版本     | v{版本号}           |
| 生成日期 | {YYYY-MM-DD HH:mm}  |
| 负责人   | {从输入中提取或留空} |

## 📚 版本历史

| 版本  | 日期       | 主要变更           |
| ----- | ---------- | ------------------ |
| v1.0  | {生成日期} | 初始版本           |

---

## 1. 背景与目标

### 1.1 业务背景

> ⚠️ **必须从 `01_Input/Context.md` 提取**，严禁臆造历史背景。

{从 Context.md 中提取以下内容：
- 这个功能/需求的历史沿革
- 之前做过的相关决策
- 遗留问题或技术债}

**来源引用**：
- {列出 Context.md 中引用的原始文档名称}

### 1.2 核心目标

{从新需求文件中提取用户想要达成的目标}

### 1.3 成功指标

{如原文提及量化指标则填写，否则标记 `> ❓ 待补充：成功指标未定义`}

---

## 2. 现状逻辑 (Code Truth)

> ⚠️ **必须基于当前代码逻辑填写**，严禁臆造。如未提供代码文件引用，此章节标记为"待扫描"。

### 2.1 当前状态机 / 流程

{扫描用户 @ 引用的代码文件，提取：
- 现有的状态枚举/流程分支
- 关键方法的处理逻辑
- 用 Mermaid 绘制现有流程图}

```mermaid
{根据代码生成流程图，如：
stateDiagram-v2
    [*] --> 状态A
    状态A --> 状态B: 触发条件
    状态B --> [*]
}
```

### 2.2 核心参数与限制

{扫描代码中的常量定义、配置项、校验规则：}

| 参数名 | 当前值 | 代码位置 | 说明 |
| ------ | ------ | -------- | ---- |
| {参数} | {值}   | {文件:行号} | {作用} |

### 2.3 现有接口 / 数据结构

{如涉及已有接口，列出关键字段。仅列出与本需求相关的部分。}

---

## 3. 功能详细设计

### 3.1 功能概述

{一句话描述新功能做什么}

### 3.2 流程图 (新逻辑)

```mermaid
{根据需求生成新的流程图：
flowchart TD
    A[开始] --> B{判断条件}
    B -->|是| C[处理A]
    B -->|否| D[处理B]
    C --> E[结束]
    D --> E
}
```

### 3.3 功能模块拆解

#### 3.3.1 {模块/页面名称}

**功能说明**：
{描述用户可以做什么}

**业务规则**：
- {权限、限制、条件}

**字段定义**：

| 字段名 | 类型 | 来源 | 逻辑校验 (基于代码) | 说明 |
| ------ | ---- | ---- | ------------------- | ---- |
| {字段} | {类型} | {来源} | {现有代码中的校验逻辑} | {说明} |

**交互规则**：
- {用户操作和系统响应}

---

## 4. 变更对比

> 清晰展示新旧逻辑的差异

| 维度 | 现状 (基于代码) | 改版后 | 变更原因 |
| ---- | --------------- | ------ | -------- |
| {维度} | {当前逻辑} | {新逻辑} | {为什么改} |

---

## 5. 限制规则

{从需求文档和代码中提取所有数字、限制、约束条件}

| 规则项 | 值 | 来源 | 备注 |
| ------ | -- | ---- | ---- |
| {规则} | {值} | 需求/代码 | {说明} |

---

## 6. 异常流程与边界处理

| 场景 | 触发条件 | 处理方式 | 用户提示 |
| ---- | -------- | -------- | -------- |
| {场景} | {条件} | {处理} | {提示语} |

---

## 7. 遗留/待定事项

{将 _WORK_SHEET.md 中未回答的问题归档到这里}

| 序号 | 问题 | 影响范围 | 建议决策时间 |
| ---- | ---- | -------- | ------------ |
| 1    | {问题描述} | {影响哪些功能} | {建议何时决策} |

---

## 附录

### A. 代码引用清单

{列出本 PRD 引用的所有代码文件}

| 文件路径 | 引用章节 | 提取内容 |
| -------- | -------- | -------- |
| {路径}   | {章节}   | {提取了什么} |

### B. Context.md 原文摘录

{将 Context.md 中的关键段落原文附在此处，便于追溯}
